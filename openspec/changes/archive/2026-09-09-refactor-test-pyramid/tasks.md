## 1. Стабилизированный вход и baseline

- [x] 1.1 Зафиксировать стабильную рабочую копию актуальных SW surfaces и пересекающихся tests без конкурирующего writer; при необходимости использовать отдельную копию проверенного среза, сохранив его незакоммиченные изменения. Перечитать `AGENTS.md` и specs, проверить `openspec validate refactor-test-pyramid --strict` и `node scripts/check-openspec-semantics.mjs`, записать source identity в `evidence/performance.md`. Release/archive `simplify-task-state-wait` не требуется; независимый drift после фиксации требует нового mapping/gates/baseline, без отката predecessor.
- [x] 1.2 Для `PYR-1` «Выбор уровня детерминированной проверки» составить `evidence/scenario-map.md` по телам всех текущих test entrypoints, включая deterministic helpers в release/eval: каждому существенному сценарию назначить уровень, итогового owner и keep/move/merge/remove. Проверить отсутствие потерянных assertions и новых semantic duplicates независимым review таблицы.
- [x] 1.3 Установить причину ambient stderr interference, получить воспроизводимое поддерживаемое Node-окружение без ослабления CLI assertions. До test/runtime edits выполнить три успешных baseline command sequences из design через существующий supervisor, включая точный whitelist переносимых нижнеуровневых tests из release/eval. Проверить каждый выбранный subprocess dependency: unguarded `credential-free Codex client observes only the package-bootstrap-installed skill` и другие actual CLI/provider cases в baseline не входят. Сохранить команды, атомарные results, TAP, executed scenarios/counts/skips, wall time и median/range. Неуспешные старые замеры оставить только диагностикой.

## 2. Разделение supervisor routes

- [x] 2.1 Реализовать `PYR-1` «Выбор уровня детерминированной проверки»: фиксированные непересекающиеся component/integration lists, их вычисляемый `unit` union и тот же полный набор `coverage`. Проверить наблюдаемый выбор child tests и отсутствие повторного исполнения через существующий supervisor test owner; не добавлять второй runner или повторный product manifest.
- [x] 2.2 Допустить `component`/`integration` в CLI и exported invocation с существующими selectors. Проверить successful focused run, cross-level rejection до spawn и zero-test failure; сохранить `NTS-1` «Терминальный foreground verdict», `NTS-2` «Диагностика текущего прогона без console шума» и `NTS-3` «Lane selection и coverage scope» через текущие supervisor regression cases, включая запрет focused coverage.
- [x] 2.3 Реализовать `PYR-2` «Изоляция live opt-ins по выбранному eval-пути»: удалить live Claude entrypoint из локального агрегата, допустить его в focused eval, очищать все admitted enable-флаги локальных/release runs, проверять совместимость enabled flags с каждым выбранным eval-файлом. Проверить child environment, отсутствие flags по умолчанию, incompatible/no-selector rejection, несколько selectors и несколько flags на doubles без запуска live сервисов.

## 3. Перенос сценариев и helpers

- [x] 3.1 Разделить runtime component validation и process-integration группы admission, interactions, lifecycle и results по scenario map. Вынести только общие test helpers без регистрации tests; сохранить последовательность global mutations и независимые roots/env. Проверить каждую новую группу через admitted focused supervisor, затем полный `unit` и `coverage` по `NTS-3`.
- [x] 3.2 Разделить mixed bootstrap, app-server, supervisor и model-discovery checks по фактическим dependency boundaries. Сохранить реальные spawn/pipe/signal/close и adapter cases; сопоставить каждый перенесённый сценарий с map. Проверить focused component/integration runs без нулевого числа tests и полный агрегат с coverage после переноса.
- [x] 3.3 Разделить eval matrix/closeout/runner/oracle helpers и их process/filesystem checks; извлечь pure tests из `release-e2e` и `codex-client-integration` в локальные уровни. Не импортировать test entrypoints как reusable modules и не копировать assertions между уровнями. Проверить focused уровни, helper import graph и полный агрегат с coverage.
- [x] 3.4 Сохранить минимальный installed-package E2E, real-Codex/hosted entrypoints и собственные recovery boundaries; real-Codex tests без guard оставить на explicit real-Codex пути с соответствующим enable, не в deterministic aggregate. Обновить focused patterns в callers, если соответствующие tests перенесены. Проверить `node scripts/run-node-tests.mjs release`, детерминированные focused eval-вызовы и routing tests `runHarness`, включая package-сценарий и отсутствие реального CLI без enable; изменение flag не подменяет проверку совместимого пути.

## 4. Сокращение измеренных издержек

- [x] 4.1 Повторно проверить mapping кандидата `MCP tools/call wires one complete session through stdio JSON-RPC`, включая default-timeout wait и stale answer. При сохранении всех существенных assertions у transport/runtime owners удалить дубль; иначе оставить недостающую проверку у соответствующего owner. Перед объединением repeated-pending tests сравнить `withFake` и `withInjectedFake`: сохранить самостоятельную adapter/env boundary, если она есть, либо отменить merge. При merge сохранить status, pending equality, wait_timeout и отсутствие progress. Проверить затронутые focused tests и полный агрегат с coverage; записать фактическую экономию, не приписывая ей ×2.
- [x] 4.2 Заменить реальные ожидания в двух измеренных stderr-drain/deadline cases управляемым срабатыванием после writer readiness. Сохранить EPIPE, stable diagnostics, late-write и cleanup assertions, не менять product limits. Проверить воспроизводимые focused runs и полный агрегат с coverage; если существующего test seam недостаточно, не вводить широкий runtime scheduler в этом change.
- [x] 4.3 Проверить раздельность fixture paths и mutable state, сравнить file concurrency 2 и 4 после разбиения; выбрать устойчивый вариант существующего registry по полным успешным прогонам. Для closeout измерить bounded parallel создание независимых файлов, сохранив последовательные mutations одного bundle. Подтвердить отсутствие flakes, leaks и потери сценариев полным агрегатом и coverage; отказаться от оптимизации без измеримого выигрыша.

## 5. Consumers, evidence и совместимость

- [x] 5.1 Обновить `EVALUATOR_INPUTS` для каждого перенесённого/нового transitive import/read/spawn input либо указать его существующего отдельного evidence owner; расширить текущий digest-mutation test на новые non-skill inputs. Проверить, что изменение каждого такого input меняет evaluator digest при неизменном skill digest; reviewer сверяет inventory с фактическим dependency graph.
- [x] 5.2 Обновить документацию реализованных `component`/`integration` routes и сохранённого `unit` aggregate, явных live/eval selectors и coverage scope, ссылаясь на supervisor как executable owner. Проверить примеры через допустимые help/selection cases без live вызовов и `git diff --check`; не копировать проектную governance из `AGENTS.md` в specs.
- [x] 5.3 Перепроверить consumer пути реального Codex и package harness после extraction. Выполнить отдельный credential-free provisioned real-Codex acceptance, если его dependency graph изменён; сохранить результат отдельно от unit/coverage. Если нужный runtime недоступен, оставить эту task и readiness gap открытыми. Hosted corpus/model rerun не запускается автоматически: старые evidence остаются привязаны к прежнему digest, без заявления о новом hosted baseline.

## 6. Итоговая проверка и закрытие

- [x] 6.1 Выполнить новые `component` и `integration`, полный `unit`, `coverage` и отдельный `release` в foreground через supervisor. Проверить terminal results, соответствие scenario map и неизменный product-source scope; выполнить полный raw-zero audit по `AGENTS.md` и устранить meaningful gaps. Проверить локальные gates также с установленными live flags на credential-free doubles/изолированном окружении.
- [x] 6.2 Собрать три успешных candidate command sequences на той же host/Node базе и эквивалентном наборе сценариев, сохранить все artifacts и сопоставить с baseline из 1.3. Подтвердить median ≤ 0,5 baseline и непересекающиеся диапазоны; отдельно сообщить component feedback и стоимость остальных обязательных gates. Если цель не выполнена, не закрывать change и не исключать проверки ради метрики.
- [x] 6.3 Провести независимый итоговый review: один test owner, отсутствие brittle timing/structural assertions и shared-state races, полнота evaluator inventory, сохранение `NTS-1`/`NTS-2`/`NTS-3`, только принятые `PYR-1`/`PYR-2` изменения supervisor и отсутствие product semantic drift. Классифицировать все findings по `AGENTS.md`; architect подтверждает минимальность исправлений. Сохранить verdict и остаточные ограничения в evidence.
- [x] 6.4 Перед archive повторить `openspec validate refactor-test-pyramid --strict`, `node scripts/check-openspec-semantics.mjs` и `git diff --check`; проверить актуальность predecessor/main и выполнение всех acceptance tasks. Archive выполнять только по отдельному запросу, не считать готовность planning artifacts завершением реализации.
