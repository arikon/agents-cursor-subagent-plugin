## 1. Проверить исходный контракт и adapter

- [x] 1.1 Перед apply сверить source digests MD-2–MD-5 с main и пересекающимся `simplify-task-state-wait`; при drift минимально rebase delta. Проверка: strict/semantic gates и записанный verdict без baseline_violation.
- [x] 1.2 Для MD-1 «Получение моделей Cursor через MCP» закрепить подтверждённый официальный SDK/API response/auth отдельной version-specific golden fixture с ожидаемым ModelCatalog; проверить полное распознавание, UTF-8/JSON, duplicate ID, malformed известные поля, пустой каталог, unsupported omission, подтверждённый uniform effort mapping и fast/optimize_for IDs, provider default стратегии и отсутствие синтеза default без зависимости от текущего состава моделей аккаунта.

## 2. Runtime и диагностика

- [x] 2.1 Реализовать MD-1 и MD-2 «Публичный MCP tool contract» в существующем runtime/adapter: exact `{}` input, success shape и errors без session allocation; добавить optimize_for в start/delegate/resume и их bootstrap/status output, проверить все три стратегии и invalid model/strategy combinations до allocation через подтверждённый adapter golden и наблюдение фактически выбранной стратегии; принятие argv без совпадения selected response не PASS. Для resume использовать существующую persisted test session с отличающейся исходной стратегией и проверить фактический currentValue после session/load; отказ загрузки пустой сессии не закрывает эту проверку. Проверка: targeted runtime tests и MCP tools/list/schema smoke через supervisor.
- [x] 2.2 Применить MD-3 «Нормативные limits runtime» к discovery: HTTP timeout, response payload bound отдельно от public inputs, один transient slot, cleanup при HTTP/network/schema/overflow и shutdown. Проверка: поведенческие tests timeout/body streaming, конкурирующего запроса, abort и освобождения ресурсов; read-only apiKey из auth file, actionable missing/invalid-file/key errors и отсутствие credentials/raw body/exception text в errors. Нет CLI/internal/environment fallback, auth writes, retries или redirect.  Без очереди/registry/cache.
- [x] 2.3 Сверить и завершить черновой stderr patch по MD-4 «Provider errors are bounded and classified»: version probe и ACP init, причина при EOF/exit, Unicode-safe truncation, ограниченное дочитывание pipe, отсутствие накопления после success/tombstone. Проверка: targeted failure tests; черновые предыдущие PASS не заменяют актуальную проверку полного MD-4.

- [x] 2.4 Реализовать MD-6 «Согласованный выбор модели до prompt» в одном resolver discovery/launch. Проверка: canonical-only ID и alias rejection; все три effort mapping без синонимов/healing; explicit fast false; полный Grok; GPT fast/context candidate вместо default overlay; no-candidate и metadata ambiguity; default-policy bypass без knobs; fresh lookup через существующие MD-3 bounds. Проверить actual canonical/каждый explicit knob после new/load, missing/mismatch cleanup без turn и без первого delegate prompt; resume использует существующую persisted session. Exact adapter schema/metadata/encoding подтверждаются primary/installed evidence и golden fixture.
  Проверить также mismatch любого encoded параметра при доступном evidence, допустимое отсутствие скрытого singleton evidence, authoritative selection response после new/load в существующем init deadline и failure teardown без live/prompt. Не добавлять polling/retry без подтверждённого adapter-протокола.
  Golden покрывает explicit canonical ID без knobs: unique default/единственный variant и fail-closed при неоднозначности, без отдельного slug bypass. Неоднозначный effort mapping проверяется как отказ всей операции discovery по MD-1, без частичной проекции. Actual evidence сравнивается с выбранным encoded variant, не с default overlay.

- [x] 2.5 По явному уточнению пользователя реализовать MD-7 «Неинтерактивная авторизация запуска» и ссылку MD-8 «Продолжение Cursor-сессии» на общий startup: file apiKey работает без browser-login и удаления файла; native token-only путь сохраняется, invalid auth даёт безопасный отказ. Проверка: installed adapter evidence, fixture file preservation/new/load/deadline и независимое review; exact child environment не переносится в skill.

## 3. Caller и пакет

- [x] 3.1 Обновить MD-5 «Skill workflow делегирования»: MCP discovery для списка/неизвестного ID, точный выбор и явная стратегия Auto по MD-2, сообщения о failure diagnostic, без CLI обхода и fallback launch. Проверка: независимый семантический аудит installed SKILL.md относительно tool names/schema и всех MD-5 сценариев; он не объявляется model-behavior eval.
- [x] 3.2 Расширить credential-free package E2E обнаружением и вызовом нового инструмента из установленного payload: success/failure до allocation, передача выбранного fixture ID и Auto стратегии в launch и доставка startup-причины. Проверка: supervisor release; нет дублирования runtime parser/resource edge cases и расширения closed eval grammar.

## 4. Приёмка реализации

- [x] 4.1 Провести независимое review кода и тестов по frozen MD baseline, классифицировать все находки и минимально исправить blockers. Проверка: reviewer verdict и подтверждение architect для классификаций, без незаявленного scope.
- [x] 4.2 Выполнить `node scripts/run-node-tests.mjs coverage`, проверить финальный TAP/exit 0 и классифицировать весь raw uncovered report согласно AGENTS.md; выполнить отдельный `node scripts/run-node-tests.mjs release` и целевые deterministic eval tests. Проверка: atomic result.json каждого foreground lane, исправленные meaningful gaps и отсутствие дубликатов tests.
- [x] 4.3 Проверить изолированно установленный payload MCP на авторизованном API-key account: discovery до сессии и отказ неизвестного ID с `invalid_args` до allocation по MD-6; затем live init с файлом только apiKey по MD-7, без browser-login и изменения auth file, без ревью/prompt. Проверка: fresh tools/skill discovery, cleanup; исходную startup-причину проверяют deterministic runtime/package fixtures. Это не обновление managed installation и не утверждение hosted model review или полного hosted eval baseline.
- [x] 4.4 Перед sync/archive повторить strict/semantic gates, убедиться в согласованности shipped runtime и skill и закрыть все MD tasks. Проверка: scope diff содержит только этот change; release/install выполняются при отдельном запросе после приёмки.
