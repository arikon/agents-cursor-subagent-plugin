## Why

Use the repository name `agents-cursor-subagent-plugin` consistently in both agents.

## What Changes

Align plugin IDs, marketplace IDs, managed paths and CLI examples. Document the
`cursor-agent` executable already used by the runtime. No lifecycle changes.

## Capabilities

### Modified Capabilities
- `cursor-plugin-distribution`
- `claude-code-plugin-distribution`

## Impact

Manifests, bootstrap identifiers, installation documentation and their fixtures.
Existing archived evidence retains its original bytes and IDs.
