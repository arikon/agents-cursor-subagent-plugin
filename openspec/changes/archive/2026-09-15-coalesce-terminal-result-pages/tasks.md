## 1. Baseline и интеграция

- [x] 1.1 Перед apply сверить source digests RP-1–RP-7 и source code с main; проверить статус compact-cursor-subagent-skill и выбранный порядок интеграции из design. При drift выполнить минимальный rebase. Проверка: записать согласованный source snapshot, сохранить обе registry entries при merge, пройти strict/semantic gates без изменения архивированных requirements.
- [x] 1.2 Сохранить baseline измерения доставки на исходном runtime/skill с fixtures из design и map существующих test assertions. Проверка: три завершённых одинаковых foreground прогона с environment, scope, concurrency, text/wire bytes, calls и digests; данные сохранены до изменения реализации, не смешаны с compact skill-body metric.

## 2. Runtime и MCP

- [x] 2.1 Реализовать RP-2 «Полное чтение terminal result» и RP-3 «Нормативные limits runtime» одним построителем страницы над existing turn. Проверка: расширить existing tests/runtime-callbacks-results.test.mjs для empty, nominal−1/nominal/nominal+1, maximum−1/maximum/maximum+1, Unicode boundary и объединения хвоста; exact text/digest, offset validity и EOF сохранены, derived-text cap не изменён.
- [x] 2.2 Реализовать RP-1 «Публичный MCP tool contract»: terminal wait result_page вместо result, existing status preview/receipt без переопределения. Проверка: existing runtime и tests/mcp-transport.test.mjs подтверждают один экземпляр текста, wait-page = read(offset:0), null отдельно от empty, nonterminal field omission; completed EOF-page с truncated preview receipt проходит без смешения digest domains.
- [x] 2.3 Сохранить RP-4 «Адресуемое ожидание состояния сессии» при изменении projection. Проверка: tests/runtime-lifecycle.test.mjs подтверждает repeated/parallel wait, loss recovery, pinned old turn при следующем terminal turn, новый unknown_turn read; existing retention/close/eviction/overflow tests не теряют assertions и показывают отсутствие новых events/provider calls/timer refresh.

## 3. Evidence и workflow

- [x] 3.1 Адаптировать recorder по RP-6 «Eval transcript plumbing и process verdict», включая first wait-page, EOF в wait и отдельный receipt proof. Проверка: existing tests/cursor-skill-eval.test.mjs проверяет contiguous received bytes/hash, IDs/total/digest mismatch, gap, offset-zero restart, nonzero replay и отсутствие delivered proof от withheld response; без raw text в compact diagnostics.
- [x] 3.2 Обновить existing scenario/loss observers по RP-7 «Сценарный контракт поведения и authority-aware interaction». Дополнить existing tests/fixtures/fake-acp.mjs provider evidence отдельным preview digest/truncation из фактически отправленного accepted agent-text (progress + result), сохранив full digest; observer использует эту проекцию только для receipt proof. Проверка: component tests для short/truncated receipt (включая progress и UTF-8 suffix boundary), short EOF-page, long continuation, loss/repeated same page и corrupted proof проходят; все прямые consumers старого result проверены, overflow не создаёт successful result-read. При необходимости согласовать только затронутые existing trace expectations, не ослабляя oracle и не реконструируя observations из expected trace.
- [x] 3.3 Внести targeted hunk в текущий (при наличии — уже compact) skill по RP-5 «Skill workflow делегирования». Проверка: independent operator review подтверждает принятие first page и только necessary next_offset reads до verification/new turn/close, прежние authority/recovery/disclosure и отсутствие копий numeric bounds/schema/receipt algorithms в skill.
- [x] 3.4 Расширить existing client-integration long-result composition и minimal installed package canary. Проверка: tests/codex-client-integration.test.mjs сохраняет tail marker, causal IDs, no regeneration и close ordering; tests/release-e2e.test.mjs реально исполняет installed payload с coalesced full result и согласованным skill/runtime. Runtime boundary matrix выше не копируется; compact request measurement сохраняется при merge.
- [x] 3.5 Проверить transitive evaluator dependency diff и существующий EVALUATOR_INPUTS. Проверка: для каждого нового admitted non-skill input добавить inventory entry и existing digest-mutation assertion; independent review импортов/read/spawn путей подтверждает полноту без второго inventory.
- [x] 3.6 Реорганизовать current skill по «Skill workflow делегирования». Проверка: before every prompt mode/context/exact-text preflight остаётся compact, protocol-error eval prose согласовано с owner contract, independent review не находит fixture-specific prose или duplicated runtime mechanics.

## 4. Итоговая проверка и измерение

- [x] 4.1 Повторить измерение candidate из 1.2 на одинаковых fixtures и scope. Проверка: три успешных сравнимых прогона показывают полный текст без обычного повторного prefix, для 8 353 bytes один result-delivery call вместо трёх, для remaining≤maximum исчезает extra tail call; сохранить text и actual serialized MCP bytes отдельно. Короткий результат не требует extra reads; metadata overhead не скрывается. Token/latency savings не заявлять без отдельного измерения.
- [x] 4.2 Выполнить полную deterministic и coverage acceptance через node scripts/run-node-tests.mjs unit и coverage, затем deterministic eval через supervisor. Проверка: завершённые exit 0 и final TAP, сохранённые atomic results; coverage denominator и raw residual classification соответствуют AGENTS.md. Не менять tests ради численной метрики.
- [x] 4.3 Выполнить release lane отдельной foreground командой и admitted real-Codex/hosted behavior acceptance новой согласованной пары по AGENTS.md, сначала diagnostic. Проверка: сохранить atomic artifacts и точные skill/runtime/evaluator/corpus digests; новый hosted baseline только с установленной project series policy, прежние baselines не выдавать за evidence нового wire.
- [x] 4.4 Завершить independent implementation/owner/inventory review и проверить compact integration. Проверка: все findings классифицированы по AGENTS.md, baseline_violation отсутствуют, источники и final payload согласованы, skill inspected как operator contract, close/restart migration проверена; финальный статус отделяет implementation acceptance от archive/publication.
- [x] 4.5 Подтвердить «Сценарный контракт поведения и authority-aware interaction». Проверка: focused serial×3 для repaired active follow-up и result overflow на затронутых model/effort configurations; затем declared full serial×3. Luna/high, Terra/high и Terra/medium дали по 81/81 с stable digest; опубликованы только их green baseline summaries.
