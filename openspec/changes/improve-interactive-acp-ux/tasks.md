## 1. Runtime interactive diagnostics

- [x] 1.1 Реализовать `IUX-2` «Адресуемое ожидание состояния сессии» и «Нормативные limits runtime»: добавлять `resume_after_event_id` во все `cursor_wait` envelopes как exact returned high-water mark и поднять max `timeout_ms` до 180 000; покрыть unit tests для timeout, pending, terminal, retained terminal wait, exact new max и rejection выше него, включая использование returned cursor в следующем wait.
- [x] 1.2 Реализовать `IUX-1` «Публичный MCP tool contract» recovery error: для unknown/stale `request_id` на active turn возвращать only exact IDs, `last_event_id` и pending IDs/kinds без context/locations; покрыть question, plan и permission, а также отсутствие ACP answer и отсутствие automatic decision.
- [x] 1.3 Реализовать `IUX-1` «Публичный MCP tool contract» terminal receipt для terminal wait/cancel/close из bounded public result; после observed terminal wait progress redacts `last_terminal_turn`, любой close redacts retained snapshot и возвращает только receipt/IDs/state, при этом explicit status сохраняет diagnostic snapshot; покрыть hash, null result, truncation, отсутствие повторной доставки result в send/wait-other-turn/close, idempotent close и отсутствие чтения/записи Cursor-private persistent storage.
- [x] 1.4 Реализовать `IUX-4` «Явный выбор модели запуска»: передать default-normalized `auto` или explicit requested model только per-session Cursor argv; покрыть default auto, exact explicit argv propagation и отсутствие read/write или наследования глобальной Cursor-конфигурации.
- [x] 1.5 Реализовать `IUX-5` «Продолжение Cursor-сессии»: добавить `cursor_resume_session({cwd,cursor_session_id,mode,model?,effort?,fast?})`, возвращать provider `cursor_session_id` в SessionEnvelope и вызывать admitted version-specific load operation; покрыть exact ID/argv propagation, canonical-root повторную проверку, отсутствие prompt/private listing/archive read/fallback provider creation и recoverable provider-load failure.
- [x] 1.6 Реализовать `IUX-6` «Высокоуровневое создание делегирования»: `cursor_delegate` forwards optional model parameters ровно одному `cursor_start_session`, не создаёт второй session, не выбирает fallback и сохраняет существующую cleanup-семантику первого prompt.
- [x] 1.7 Реализовать `IUX-7` «Per-session параметры модели»: принять optional `effort` и `fast` в start/delegate/resume, вернуть exact values в envelope и покрыть omission, adapter fixture, forwarding и отсутствие global config mutation/fallback.
- [x] 1.8 Исключить из v1 неподтверждённый `auto_optimize_for`: pinned CLI не публикует для него отдельный flag, а documented Router contract требует catalog-discovered `auto-smart[optimize_for=...]`, которого нет в installed model catalog.
- [x] 1.9 Исключить fake-only `cursor_steer`: pinned installed ACP не рекламирует steering capability и не содержит подтверждённого wire method; уже переданный active-turn follow-up ждёт terminality и продолжает ту же live session через `cursor_send_prompt`.
- [x] 1.10 Реализовать `IUX-11` «Seamless per-session launch» seamless implementation budget: поднять default bounded lifetime turn с 600 000 до 3 600 000 ms и покрыть отсутствие premature cancellation до нового cap; сохранить closed launch schema, а exact исключённые provider controls — только в version-specific adapter/golden evidence.
- [x] 1.11 Реализовать `IUX-12` timeout progress digest: `cursor_wait` на timeout принимает optional caller-held `after_progress_revision`, возвращает revision и at most 512-byte bounded excerpt только если accepted ACP `agent_message_chunk` продвинулся; без delta не повторяет excerpt, raw transcript/thinking/tool payload не публикует. Покрыть repeated timeouts, truncation, new progress и отсутствие private archive access.
- [x] 1.12 Реализовать `IUX-13` «Sparse wait and bounded progress» sparse wait delivery: заменить duplicated `WaitEnvelope extends TurnEnvelope` на flat `WaitDeltaEnvelope`; always include IDs, state, `last_event_id`, `resume_after_event_id`, `wait_timeout` and `events_lost`, а events/pending/result/reason/receipt/progress включать only when nonempty or changed. Исключить model/run-mode/sandbox/full nested snapshots и semantic `null`/empty arrays из normal wait; `cursor_session_status` остаётся full diagnostic owner. Покрыть exact omission и terminal single delivery.
- [x] 1.13 Реализовать `IUX-14` «Provider errors are bounded and classified» ACP error normalization: JSON-RPC provider `error` на init/prompt/load классифицировать отдельно от malformed frame/EOF/spawn и возвращать bounded `provider_error:{code,message}` без raw `data`; terminalize only affected runtime session/turn, ignore late response after terminality и покрыть error-without-message/invalid error envelope.
- [x] 1.14 Реализовать `IUX-15` plugin delivery: optional `plugin_dirs?:string[]` в start/delegate/resume canonicalizes nonempty absolute local plugin roots inside allowed roots and exactly forwards repeated `--plugin-dir <root>` only to that ACP process. Session envelope returns canonical roots; tests cover skills/MCP bundle fixture, order, omission, invalid/outside path and no global config/session-new raw MCP mutation.
- [x] 1.15 Реализовать `IUX-16` «Режимы Cursor и ACP callbacks» и `IUX-16`/`IUX-17` «Role-neutral mode and collaboration surface»: `cursor_set_mode({session_id,mode})` допускает exact `ask|plan|agent` only for a live session without active turn, forwards admitted adapter mode operation, returns sparse `ActionEnvelope` + mode и не allocates turn/session или не меняет launch model/plugin configuration. ACP process advertises fixed read/write callbacks at initialize, но runtime допускает write только в current `agent`; покрыть ask→agent write, ask rejection, valid transition, same-mode no-op, active/tombstone rejection и provider/unadmitted-result terminalization.
- [x] 1.16 Реализовать `IUX-17` compact collaboration extension requests: normalize admitted version-specific requests to bounded typed runtime events visible through `cursor_wait`; retain no raw prompt, image bytes, filesystem contents or provider envelope, make exactly one empty-result response attempt for every recognized request ID while transport is writable, and ignore malformed/unadmitted requests without terminalizing a healthy turn. Pre-write non-writable/synchronous rejection is abandoned without retry or session/turn rewrite; async EPIPE after write acceptance remains the existing fail-closed transport failure. Покрыть todos merge/replace, task completion, image path suggestion, byte bounds, omission и the admitted response attempt.
- [x] 1.17 Расширить IUX-1 на compact mutation acknowledgements: `cursor_delegate`, `cursor_send_prompt`, answer-tools, `cursor_set_mode`, `cursor_cancel` и `cursor_close_session` возвращают только IDs, state, relevant outcome/receipt и необходимый bootstrap model echo у delegate; nested snapshots, static launch fields, null и empty collections остаются только у explicit `cursor_session_status` и start/resume bootstrap. Покрыть exact sparse shapes и отсутствие повторной terminal result.

## 2. Skill workflow and observed behavior

- [x] 2.1 Реализовать `IUX-3` «Skill workflow делегирования» в установленном `cursor-subagent` skill: добавить mutually exclusive templates `file review` и `snapshot review` с допустимым `mode`, optional per-session `model`, returned `cursor_session_id` для explicit resume, state handoff exact IDs/resume cursor, terminal-receipt reporting, active-turn follow-up recovery only after completed+live, recovery unknown pending ID и fail-closed report без wait/retry/fallback для initial init/spawn tombstone или live-turn provider rejection; проверить package skill digest/discovery и отсутствие новых low-level workflow tools.
- [x] 2.2 Расширить credential-free client-integration/eval scenarios observable assertions для resume cursor, recovery error, failed provider turn, active-followup race, failed mode transition и terminal receipt; убедиться, что protocol completion по-прежнему не объявляется semantic task success и permission authority policy не меняется.
- [x] 2.3 Реализовать `IUX-9` «Budgeted critic-review workflow»: skill формирует compact review manifest с digest, при повторном review передаёт только delta и использует bounded exponential wait backoff; покрыть отсутствие повторной передачи неизменённого snapshot и сохранение exact session/turn IDs.
- [x] 2.4 Дополнить happy-path skill: сохранять existing per-request authority workflow и не эмулировать unlisted provider controls. В terminal report разделять completion, timeout и resumable work-in-progress без ложного semantic-success claim.
- [x] 2.5 Ограничить IUX-11: без уже выданной exact/bounded delete authority skill сохраняет returned/observed Cursor-created temporary artifacts и сообщает пользователю для отдельного решения; существующая explicit authority не требует повторного подтверждения, а невидимые provider-internal paths не объявляются обнаруженными.
- [x] 2.6 Дополнить skill: при user-provided Agent Plugin bundle передавать `plugin_dirs` rather than copying `SKILL.md` into checkout or emitting raw provider config. Документировать отсутствие другого admitted public per-session delivery path.
- [x] 2.7 Дополнить role-neutral happy path: выбирать `ask` для любой read-only работы, включая diagnosis/debugging, `plan` для согласования плана, `agent` только для явно авторизованной write-capable implementation/debugging; при смене фазы сохранять same session через `cursor_set_mode`, а `cursor_wait` показывает compact todo/task/image progress. Failed mode transition сообщает exact error и следует state-based recovery из owner requirement «Skill workflow делегирования»: locally malformed `invalid_args` допускает однозначное исправление, `protocol_error` проверяет live state и сохраняет idle session либо только сообщает unexpected active state без новой provider operation, provider failure/tombstone требует нового user decision. Skill не трактует collaboration extension request или progress event как completion и не выдаёт permission decision автоматически.

## 3. Contract verification

- [x] 3.1 Обновить project semantic registry и version-specific adapter/golden fixtures для additive public envelope fields, installed load/mode contract и collaboration extension requests; исключить неподтверждённый steer wire; запустить targeted runtime, facade, MCP transport, skill-eval и client-integration tests.
- [x] 3.2 Запустить `openspec validate improve-interactive-acp-ux --strict` и project semantic gate; проверить, что owner map не дублирует runtime state machine или permission policy.
- [x] 3.3 Запустить foreground `node scripts/run-node-tests.mjs unit`, `node scripts/run-node-tests.mjs coverage` и `node scripts/run-node-tests.mjs release`; прочитать raw coverage и классифицировать residual zero-count counters по правилам проекта.
- [x] 3.4 Реализовать `IUX-18` как изменение owner `node-test-supervision` «Lane selection и coverage scope»: focused `--test tests/name.test.mjs` и `--test-name-pattern` используют тот же process-group/artifact supervisor; dedicated `eval` lane сохраняет только explicit eval opt-ins, coverage остаётся полным manifest lane. Зафиксировать в AGENTS.md запрет direct `node --test` и покрыть parser/argv contract.
- [x] 3.5 Реализовать `IUX-19` только в owner `cursor-subagent-skill-evals` «Разделённые eval lanes и evidence загрузки skill», «Сценарный контракт поведения и authority-aware interaction», «Cost-aware execution policy» с project-wide policy из `AGENTS.md`: выводить counts только из corpus, для нового MCP/skill happy/recovery path добавить scenario eval, установленный `SKILL.md` проверить как актуальный operator contract по admitted tools, mode/authority и close/resume guidance, а hosted cleanup считать successful только после observed child close. Текущая реализация одного table-driven pass остаётся в `tests/run-cursor-skill-eval.test.mjs` с runaway timeout 2000 ms, а pure scenario surface — в `scripts/cursor-eval-scenario.mjs`; это task-level layout, не публичный контракт.
- [x] 3.6 Изменить `IUX-18` «Проверяемая чистая установка»: сохранить package-owned canary semantics, но сделать `node scripts/run-node-tests.mjs release` единственным driver через owner `node-test-supervision`.
- [x] 3.7 По явному запросу учесть все findings расширить `IUX-18` «Managed marketplace lifecycle»: включить устанавливаемый `scripts/recording-mcp-proxy.mjs` в owner allowlist/hash domain и проверить byte-transparent bounded framing, fatal observation decode и flush final response under backpressure.

## 4. Review remediation

- [x] 4.1 Вернуть complete normalized pending context в sparse `cursor_wait` и удалить test-only diagnostic status bridge; перевести runtime, transport и eval evidence на реальный flat envelope.
- [x] 4.2 Изменить `IUX-3` «Workspace discipline делегирования»: isolated worktree с обязательной precondition на рекомендацию для write-capable/concurrent delegation и исключить automatic artifact cleanup из v1.
- [x] 4.3 Уточнить multi-turn lifecycle: live session продолжается через `cursor_send_prompt`, `cursor_set_mode` работает между turns, close относится к completion/abandonment/irrecoverable failure либо к required launch-setting change с immediate explicit resume.
- [x] 4.4 Зафиксировать непротиворечивые file-review и snapshot-review templates, wait-timeout/terminal-timeout distinction, bounded backoff, authority boundary и critic delta handoff.
- [x] 4.5 Удалить substring-based operator skill assertion и заменить его Codex behavior scenarios «Сценарный контракт поведения и authority-aware interaction» с exact tool/effect traces для pending handoff, file/snapshot review и multi-turn continuation.
- [x] 4.6 Обновить package version, установить новый personal plugin build и доказать, что Codex загрузил exact installed skill/MCP payload.
- [x] 4.7 Выполнить skill validation, targeted tests, strict/semantic OpenSpec gates, full unit/coverage/release и классифицировать residual coverage counters.
- [x] 4.8 Выполнить enabled Codex behavior evals и независимый финальный review всех remediation findings.
