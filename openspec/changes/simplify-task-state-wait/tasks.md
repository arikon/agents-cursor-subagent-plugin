## 1. Согласование baseline

- [x] 1.1 Сверить полные delta blocks с текущими owner specs: четыре из archived `improve-interactive-acp-ux`, пять из main (три SW-6 и два перебазированных MD пересечения); подтвердить наличие требуемых runtime/skill/eval surfaces в checkout, отсутствие потерь unrelated требований и stale cursor/delivery правил в successor. Согласовать полные MCP и skill workflow delta с `add-cursor-model-discovery`: сохранить его новые инструменты и поля, включая `cursor_list_models`, `optimize_for` и ModelCatalog, через ссылки на их owner; старая полная таблица SW не должна откатить model discovery. Завершение подтвердить повторными gate/review на текущем main.
- [x] 1.2 Добавить sourceChange/digest admission в существующий semantic gate и зарегистрировать все девять replacement blocks. Проверить fail-closed active/archived source, неверные ссылки, отсутствующий block и drift через `node scripts/run-node-tests.mjs unit --test tests/check-openspec-semantics.test.mjs`; затем `node scripts/check-openspec-semantics.mjs` и `openspec validate simplify-task-state-wait --strict`. Main specs и состояние predecessor не менять.
- [x] 1.3 Провести независимый critic review и architect validation классификаций по AGENTS.md; сохранить verdict с каждой находкой, её единственной классификацией и repair owner; freeze только без baseline_violation.

## 2. Runtime

- [x] 2.1 Обновить «Публичный MCP tool contract» (SW-1): schema и envelope; проверить rejected legacy arguments и JSON transport через `node scripts/run-node-tests.mjs unit --test tests/mcp-transport.test.mjs`, не дублируя runtime state assertions.
- [x] 2.2 Реализовать «Адресуемое ожидание состояния сессии» (SW-2) поверх existing turn; удалить consumer delivery state для wait. Проверить observable running timeout, current pending после answer, повторный terminal всех outcomes, два concurrent readers, wake/timeout race, immediate next turn и expired address через `node scripts/run-node-tests.mjs unit --test tests/runtime.test.mjs`; каждый сценарий имеет один test owner. Конкретный regression (`implementation_concern`, уже покрыт SW-2): до answer одновременно существуют два permission requests; после ответа на первый второй остаётся pending. Без новых событий следующий wait немедленно возвращает оставшийся request с `wait_timeout:false`, не ожидая истечения timeout. Добавить сценарий в существующий runtime test owner, см. [evidence сессии](evidence/pending-permission-wait.md).
- [x] 2.3 Обновить «Sparse wait and bounded progress» (SW-3) и wait-facing сценарий «Role-neutral mode and collaboration surface» (SW-4): progress flood не сокращает wait, excerpt отсутствует до accepted text и обязателен после него на running timeout, повторный timeout возвращает тот же bounded excerpt, event eviction не ломает state observation. Проверить в runtime lane из 2.2; сохранить существующие adapter/golden/ack и full-result tests без дублирования публичных collaboration assertions.

- [x] 2.4 Согласовать SW-6: «Единая машина состояний runtime», «Изолированное состояние ходов и событий», «Ограниченный жизненный цикл ACP-процесса» используют state observation; сохранить transitions, isolation и retention. Проверка: existing lifecycle/isolation scenarios в runtime lane и semantic gate, без второго test owner для legacy argument rejection.

## 3. Operator workflow и consumers

- [x] 3.1 Обновить «Skill workflow делегирования» (SW-5), installed skill и README: убрать cursor bookkeeping из first/later wait, answer и stale-request recovery; сохранить authority, multi-turn, result-read и close. Проверить полный operator flow по новой admitted schema.
- [x] 3.2 Обновить «Сценарный контракт поведения и authority-aware interaction» (SW-7) и существующие сценарии corpus/driver/oracle: два argument-recovery класса, cursor-free wait и pending follow-up; удалить notification/event-burst и публичные collaboration observations. Добавить единственный `lose-terminal-wait-response-once` fault: withheld response, caller-visible failure и повторный wait проверяются по полным raw records; негативные проверки отвергают отсутствие trigger/evidence, неправильные IDs, interleaving и повторный provider effect. Проверить targeted proxy/oracle unit tests и `node scripts/run-node-tests.mjs eval --test tests/codex-client-integration.test.mjs`; сохранить общий evidence/authority policy, добавив только объявленное fault-specific proof.
- [x] 3.3 В существующей timeout/recovery row задать уникальный fixture `progress_text` и включить его literal в `report_checks.required_fragments` timeout turn. Проверить, что отсутствие literal в final даёт mismatch, а tool/commentary text не заменяет final; использовать существующие report checks без новых trace kinds или prose judge.
- [x] 3.4 Обновить credential-free package E2E для согласованной установки runtime и operator skill; проверить счастливый путь и recovery из 3.2 на package boundary через `node scripts/run-node-tests.mjs release`, без повторного доказательства runtime race semantics.

## 4. Проверка и выпуск

- [x] 4.1 Выполнить `node scripts/run-node-tests.mjs unit` и `node scripts/run-node-tests.mjs coverage` в foreground до terminal verdict; классифицировать все zero counters по AGENTS.md, устранить meaningful gaps, не снижать product manifest. В существующем scripted-сценарии из 2.2 сравнить старый и новый wait при одинаковых двух pending permissions и отсутствии новых событий после первого answer: зафиксировать старую зависимость от timeout/cursor и новый возврат pending без истечения timeout и без cursor bookkeeping. Использовать управляемое время/синхронизацию существующего test harness, без нестабильного live-порога и нового benchmark framework; не дублировать runtime assertions в другом test owner. Объём ответов допускается измерить дополнительно, без обязательного выигрыша.
- [x] 4.2 Проверить strict/semantic gates и `git diff --check`, перечитать installed operator contract; подтвердить отсутствие новых dependencies, event-reader и второй машины состояний.
- [x] 4.3 Перед выпуском провести отдельно требуемые проектом real-Codex/hosted acceptance lanes по действующему AGENTS.md; сохранить полные atomic artifacts и измеренные outcomes. Старые cursor-based baseline результаты не считать доказательством нового workflow.
- [x] 4.4 Выпустить runtime и skill согласованно с breaking-change notice и явным close/restart boundary: перед обновлением завершить старые делегирования и MCP process, затем установить пакет и открыть чистую задачу с заново загруженным skill/tools. В package acceptance доказать старую schema до close, отсутствие старого процесса после close и новую schema вместе с новым installed skill в чистой задаче; file update не считать restart. Rollback повторяет boundary и возвращает согласованную предыдущую версию обоих компонентов.
- [x] 4.5 Перед archive проверить exact main sourceDigest и archived references после завершения implementation/release acceptance этого change. IUX и MD уже sync/archive; проверить strict/semantic gates, не подменяя ими завершение SW.
