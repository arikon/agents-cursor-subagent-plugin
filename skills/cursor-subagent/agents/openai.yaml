interface:
  display_name: "Cursor Subagent"
  short_description: "Delegate a task to Cursor through interactive ACP"
  default_prompt: "Use $cursor-subagent to delegate a task to Cursor Agent through ACP."

policy:
  allow_implicit_invocation: false
