# Runtime zero-counter classification

Source: `verification/test-speed/final-series/coverage-zero-counters.json`, final-series coverage run 01 snapshot for `scripts/cursor-subagent-mcp.mjs` (931/931 lines, 863/918 branches, 170/170 functions). The run passed in 63,664 ms with 556 tests (555 passed, 1 skipped) and no coverage diagnostics. Line numbers below refer to the source frozen for that run. Repeated line numbers denote distinct raw branch counters and are retained explicitly. The raw report contains 56 zero-count branch entries; line 570 is excluded from the denominator by its justified local coverage directive, hence the aggregate denominator reports 55 uncovered branches.

| Classification | Raw zero counters | Semantic owner and disposition |
|---|---|---|
| `realistic_failure` | 82, 84, 97, 129, 683, 683, 697, 700 | Invalid adapter argv/manifest/load, plugin-root, resume-mode and shared live-capacity alternatives. Existing bootstrap failure, packaged-manifest process, plugin containment, invalid-mode and capacity tests own these public fail-closed paths. The zeros are alternate short-circuit operands or the resume entry to a shared owner; another assertion would duplicate the same contract. |
| `realistic_failure` | 132, 140, 142, 144, 145, 151, 161, 164, 165, 167, 176, 182, 184, 185, 188, 224, 227, 241, 242 | Malformed todos/task/image/question/plan and derived ID/text alternatives. Adapter golden-fixture and malformed callback tests already verify rejection without publication or terminal corruption. Individual short-circuit operands are implementation detail inside those covered semantic paths. |
| `realistic_failure` | 293, 298, 299, 299, 308, 309, 324 | Same-state lifecycle, redacted/receipt/turn envelope alternatives and empty/lost event forms. Existing sparse-envelope, idempotent close, retained receipt, event-loss and explicit-status tests own these observable variants. No new test added because the zero counters are construction branches inside those already-covered public envelope scenarios. |
| `realistic_failure` | 446, 450, 533, 537, 544, 572, 627, 628, 652 | Closed/asynchronously failing transport, collaboration notifications outside an active admitted turn, malformed extension data, filesystem callback OS failures, answer-write races, and a closed transport while best-effort cancellation replies are sent during terminalization. Existing EPIPE, post-completion callback, best-effort collaboration acknowledgement, malformed callback, exactly-once answer and transport-shutdown tests own the public outcomes. Timing and short-circuit operands do not justify duplicate assertions. |
| `realistic_failure` | 684, 785 | Eviction ordering/tie alternatives and invalid progress-cursor operands. Deterministic tombstone eviction and public wait-validation tests already own these semantics. |
| `unreachable_defensive` | 25 | `bounded` nullish-to-empty fallback: admitted callers pass normalized strings; null result bypasses `bounded`. Retained as local defensive normalization. |
| `unreachable_defensive` | 170 | Optional task `type` omission after successful admission: preceding contract rejects missing/unresolved `subagentType`, so admitted projection always has `type`. |
| `unreachable_defensive` | 270 | Missing turn passed to `terminalReceipt`: all public callers guard the turn/last-terminal presence before invoking it. |
| `unreachable_defensive` | 453, 460, 464 | Synchronous stream throws after the writable precheck in `sendConfirmed`, request send and error response. The admitted realistic outcomes are the tested closed precheck and asynchronous EPIPE paths. |
| `unreachable_defensive` | 570 | Above-cap filesystem write after the stricter ACP frame limit. JSON framing makes this branch unreachable under equal 1 MiB bounds; the adjacent coverage directive documents it and transport-level no-mutation behavior is tested. |
| `unreachable_defensive` | 610 | Synchronous prompt request failure after a live writable ACP process was admitted. Closed/EPIPE/exit paths terminalize through their tested transport owners; this catch remains fail-closed if stream behavior changes. |
| `unreachable_defensive` | 615 | Stale `complete` after the prompt RPC waiter was removed or shutdown cleared it. Synchronous result/error settlement now preserves wire order, while the guard prevents resurrection if a future caller bypasses that owner. Same-batch response→late normal/overflow and error→late tests prove the observable invariant. |
| `unreachable_defensive` | 623 | Missing pending request inside `SessionRecord.answer`: the public answer dispatcher validates and claims the pending ID before entering this method. Unknown/stale IDs are covered through the public recovery contract. |
| `unreachable_defensive` | 648 | Repeated `terminalize` after active/terminal ownership changed. Public cancel, timeout, transport failure and same-batch races prove the single immutable terminal result; a direct private call would be an artificial test. |

No counter is a currently missing meaningful contract, and none is `dead_code`. Compared with the preceding full snapshot, the plan-normalization branch at line 232 is now covered and one realistic best-effort cancellation-response branch appears at line 652; the exact raw multiset remains 56 entries. Result accumulator regressions exercise exact 1 MiB success and one-byte overflow using 4096 small chunks per turn; wire-order regressions prove one immutable terminal for response→late normal, response→late overflow and provider-error→late input. No artificial private-structure test was added.

## Final-series coverage run 02 addendum

Source: `verification/test-speed/final-series/coverage/run-02/failures.jsonl`, last `test:coverage` event. Run 02 passed in 65,714 ms with the same 556-test outcome and frozen source. Runtime coverage was 931/931 lines, 864/921 branches and 170/170 functions. Its raw queue contains the complete run-01 multiset plus two zero-count entries, at lines 220 and 232. The changing branch denominator (918 in run 01, 921 in run 02) confirms that these are run-specific V8 branch-counter enumeration differences rather than a source or public-contract change.

| Classification | Run-02-only raw zero counters | Semantic owner and disposition |
|---|---|---|
| `realistic_failure` | 220, 232 | Missing/empty question payload rejection and the plan-vs-permission normalization route. Existing malformed-question callback tests verify rejection without publication, while plan and permission callback tests verify their public normalized pending envelopes. These counters are short-circuit/routing alternatives within those semantic owners; duplicating the assertions would test instrumentation shape rather than behavior. |

Run 02 therefore adds no missing meaningful contract or dead code. No product or test change is warranted.
